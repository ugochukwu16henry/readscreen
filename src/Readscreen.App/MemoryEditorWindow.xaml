<Window x:Class="Readscreen.App.MemoryEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Personal Memory"
        Height="500"
        Width="700">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ListBox x:Name="MemoryList" Grid.Row="0" DisplayMemberPath="Title"
                 SelectionChanged="OnSelectionChanged" Margin="0,0,0,8"/>

        <Grid Grid.Row="1" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Category" Grid.Row="0" VerticalAlignment="Center"/>
            <ComboBox x:Name="CategoryBox" Grid.Row="0" Grid.Column="1" Margin="8,4,0,4"/>

            <TextBlock Text="Title" Grid.Row="1" VerticalAlignment="Center"/>
            <TextBox x:Name="TitleBox" Grid.Row="1" Grid.Column="1" Margin="8,4,0,4"/>

            <TextBlock Text="Content" Grid.Row="2" VerticalAlignment="Top" Margin="0,8,0,0"/>
            <TextBox x:Name="ContentBox" Grid.Row="2" Grid.Column="1" Margin="8,8,0,0"
                     AcceptsReturn="True" TextWrapping="Wrap" Height="120"
                     VerticalScrollBarVisibility="Auto"/>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="New" Click="OnNew" Padding="12,6" Margin="0,0,8,0"/>
            <Button Content="Save" Click="OnSave" Padding="12,6" Margin="0,0,8,0"/>
            <Button Content="Delete" Click="OnDelete" Padding="12,6"/>
        </StackPanel>
    </Grid>
</Window>
